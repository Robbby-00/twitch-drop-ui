export enum SettingKeys {
    // Apereance
    THEME = "theme",

    // Api
    API_PORT = "apiPort",

    // Tracker
    AUTO_CONNECTED_CAMPAIGN = "autoConnectedCampaign",
    UPDATE_INTERVAL = "updateInterval",
    CLAIM_INTERVAL = "claimInterval",
    MAX_CONCURRENT_CAMPAIGN = "maxConcurrentCampaign",
    MAX_CONCURRENT_WATCHER = "maxConcurrentWatcher",

    // Watcher
    WATCH_INTERVAL = "watchInterval",
    MAX_WATCH_ERROR = "maxWatchError",

    // User
    ACCESS_TOKEN = "accessToken",
    MAX_INTEGRITY_TOKEN_ERROR = "maxIntegrityTokenError",

    // Browser
    CHROME_BINARY = "chromeBinary",
    HEADLESS = "headless",
    STEALTH_MODE = "stealthMode",
    BLOCK_TRACKER = "blockTracker",
    BROWSER_TEST = "browserTest"
}